#pragma once
#include<iostream>
#include<cctype>
#include<string>

using namespace std;

class lienHe {
	public:
		static bool kiemTraSDT(const string& sdt);
		static bool kiemTraEmail(const string& email);
};
